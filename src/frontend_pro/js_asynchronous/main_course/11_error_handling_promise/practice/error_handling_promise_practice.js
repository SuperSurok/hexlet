import { promises as fs } from 'fs';

////////////////////////////////////////
//////// First Solution ////////////////
////////////////////////////////////////
export const touch = (file) => fs.access(file)
  .catch(() => fs.writeFile(file, ''));

////////////////////////////////////////
//////// Second Solution ///////////////
////////////////////////////////////////
export const touchMaster = (filepath) => fs.access(filepath)
  .catch(() => fs.writeFile(filepath, ''));